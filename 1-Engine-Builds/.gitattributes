# Keep line endings correct regardless of the machine's core.autocrlf setting.
# Shell scripts must stay LF or bash fails with "set: pipefail: invalid option name".
*.sh    text eol=lf
# Windows scripts want CRLF.
*.bat   text eol=crlf
*.ps1   text eol=crlf
